import "./styles.sass"
import { CardSlider } from "../../components/CardSlider"

export const Portfolio = () => {
    return (
        <main>
            <div className="portfolio">
                <CardSlider />
            </div>
        </main>
    )
}